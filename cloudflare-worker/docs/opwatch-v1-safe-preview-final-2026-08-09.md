# OP Watch V1 — final safe preview validation

Deploy the latest tested V1 branch in SAFE_PREVIEW only.

Required invariants:
- monitoring disabled
- state writes disabled
- Discord dry-run
- health endpoint healthy
- official One Piece release calendar reachable and parsed

This marker does not authorize live monitoring.
