name: Alert Evaluation Archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived evaluation notice
        run: echo "The one-shot alert evaluation is complete. No automatic source requests are configured here."
