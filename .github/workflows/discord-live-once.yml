name: Discord Live Test Archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived test notice
        run: echo "The one-shot Discord live test has completed and cannot send another alert."
